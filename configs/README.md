# Configurations

This directory is reserved for reviewed, portable configuration templates and examples.

## Rules

- Never commit secrets, API keys, passwords or tokens.
- Keep machine-specific values separate from portable configuration where practical.
- Record the component/version a configuration targets.
- Changes to security-sensitive configuration require review and verification.
- Configuration should be reproducible where practical.
